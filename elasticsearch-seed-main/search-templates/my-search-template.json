{
  "script": {
    "lang": "mustache",
    "source": {
      "query": {
        "match": {
          "name": "{{query_string}}"
        }
      },
      "from": "{{from}}",
      "size": "{{size}}"
    },
    "params": {
      "query_string": "My query string"
    }
  }
}